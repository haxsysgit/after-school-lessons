{
  "name": "coursework1-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git checkout main && git rm -r --cached . && git clean -fd && cp -r dist/* . && git add . && git commit -m 'Deploy build for github pages' && git push origin main --force && git checkout dev"
  },

  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.1.0",
    "bootstrap": "^5.3.8",
    "vue": "^3.5.22"
  },

  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "rimraf": "^6.0.1",
    "vite": "npm:rolldown-vite@7.1.14"
  },

  "overrides": {
    "vite": "npm:rolldown-vite@7.1.14"
  }
}
